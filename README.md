# The-Template

\_Версия 1.10.0

**Что нового в версии 1.10.0:**

**Документация:**

- 📚 Полная документация модуля Excel с 5 практическими примерами и 3 паттернами использования
- 📖 Улучшенные docstrings для 20+ публичных методов с 40+ примерами кода
- 💬 Подробные комментарии в core/onchain.py для сложных методов (\_get_l1_fee, \_validate_native_transfer_value, \_get_fee, remove_approves)
- ✨ Секции Examples и Raises во всех ключевых методах
- 🎯 Расширенная документация модуля Onchain с примерами работы без браузера
- 📊 Примеры массовой проверки балансов для всех токенов во всех сетях
- 🚀 Добавлен раздел "Быстрый старт" с пошаговой инструкцией за 10 минут
- 🤖 Добавлен AGENT.md - контекст для AI помощников с описанием архитектуры проекта

**Архитектура:**

- 🔌 Добавлен абстрактный класс Wallet для поддержки полиморфизма кошельков
- 📝 Добавлена инструкция по добавлению нового класса для кошелька (WALLET_GUIDE.md)
- 🔄 Методы Metamask переименованы для единообразия (open_wallet, auth_wallet)
- 📁 Реорганизация структуры: кошельки перенесены в core/browser/wallets/
- ✅ Обеспечена обратная совместимость через core/browser/\_\_init\_\_.py

**Исправления:**

- 🐛 Исправлен баг с номером профиля в Excel
- 🐛 Исправлена ошибка в okx.get_balance_funding
- 🌍 Metamask теперь не зависит от языка расширения (использует test_id вместо текста)
- 🔧 Улучшен метод get_response с лучшей обработкой ошибок

### Описание

**Заготовка шаблона для создания скрипта автоматизации.**

Подготовлено для студентов обучающихся у https://t.me/maxzarev

Если вы нашли баг или неточности, пожалуйста сообщите https://t.me/max_zarev

Достаточно заполнить config и запустить файл run.py, чтобы скрипт начал запускать профили в ADS.

## 🚀 Быстрый старт

Этот раздел поможет вам быстро начать работу с проектом за 10 минут.

### Что понадобится:

- Python 3.11 или выше
- Установленный ADS Power с созданными профилями и плантым тарифом для работы с API.

### Шаг 1: Проверка Python

```bash
python --version  # должно быть >= 3.11
```

Если Python не установлен, скачайте с https://www.python.org/downloads/

### Шаг 2: Установка зависимостей

```bash
# Установите зависимости проекта
pip install -r requirements.txt
```

### Шаг 3: Настройка ADS Power

1. Запустите ADS Power
2. Создайте минимум один профиль браузера
3. Запомните номер профиля (например: 12345)

### Шаг 4: Минимальная конфигурация

```bash
# Переименуйте файл с номерами профилей
cd config/data
cp profile_numbers.txt.example profile_numbers.txt # переименуйте файл с номерами профилей, удалите .example
```

Откройте `config/data/profile_numbers.txt` и добавьте номера ваших профилей:

```
12345
12346
12347
```

Откройте `config/settings.py` и измените:

```python
accounts_source = 'txt'  # Изменить с 'excel' на 'txt' для работы с текстовыми файлами
is_browser_run = True    # Включить запуск браузера
```

**Важно:** По умолчанию скрипт ищет профили в Excel файле (`accounts_source = 'excel'`). Если вы используете текстовые файлы, обязательно измените этот параметр на `'txt'`.

### Шаг 5: Первый запуск

```bash
python run.py
```

**Что произойдет:**

1. Скрипт подключится к ADS Power
2. Откроет ваши профили по очереди
3. Зайдет на Google.com
4. Закроет профили

### Поздравляем! 🎉

Базовая настройка завершена. Теперь можно:

- Добавить работу с Metamask (см. раздел "Установка")
- Настроить работу с блокчейном (добавьте приватные ключи)
- Подключить биржи OKX/Binance (настройте .env)

Для полной настройки всех возможностей см. раздел **"Установка"** ниже.

#### Возможности

- Запускает профили в ADS Power
- Установка прокси в профили ADS
- Авторизация в Metamask
- Создание и импорт Metamask
- Работа с расширениями
- Работа с всплывающими окнами
- Управление браузером ADS
- Работа из Excel файла или текстового файла
- Отчетность в Excel
- Расписание
- Выводы с биржи OKX, Binance
- Работа с балансами в EVM сетях
- Перевод токенов в ERC20 сетях
- Уведомления в телеграм

  **В планах:**

- Обмен любых ERC20 токенов в EVM сетях
- Вывод с любой биржи
- модуль шифрования и гайд

**Для запуска необходимо:**

- настроить файл config/settings.py.
- переименовать файл .env.example в .env и заполнить его.
- заполнить файлы в директории config/data
- заполнить файл config/chains.py если нужно добавить сети
- заполнить файл config/tokens.py если нужно добавить токены

#### Структура проекта:

```
project/
├── config/                      # файлы с данными для скрипта.
│   ├── data/                    # файлы с данными для скрипта.
│   │   ├── ABIs/                # технические json файлы с описанием работы смарт-контрактов.
│   │   ├── addresses.txt        # адреса кошельков для работы (если не используется файл accounts.xlsx).
│   │   ├── passwords.txt        # пароли для метамасков (если не используется файл accounts.xlsx).
│   │   ├── private_keys.txt     # приватные ключи для работы (если не используется файл accounts.xlsx).
│   │   ├── profile_numbers.txt  # номера профилей в ADS для работы (если не используется файл accounts.xlsx).
│   │   ├── proxies.txt          # proxy для установкив в профили, если надо настроить профили ADS. (если не используется файл accounts.xlsx).
│   │   ├── seeds.txt            # seed для импорта метамасков (если не используется файл accounts.xlsx).
│   │   ├── accounts.xlsx        # файл с данными для работы скрипта. (Предпочтительно использовать xlsx)
│   │   ├── user_agents.txt      # файл с user-agent для подставки в запросы, можете добавить свои.
│   │   ├── chains_data.json     # файл с выгрузкой информации по всем блокчейн сетям для поиска нужных данных.
│   ├── settings.py              # настройки скрипта.
│   ├── .env                     # приватные данные для скрипта.
│   ├── chains.py                # добавление сетей для работы скрипта.
│   ├── contracts.py             # адреса контрактов для работы скрипта.
│   ├── tokens.py                # добавление токенов для работы скрипта.
│── core                         # ядро проекта, все основные модули и классы внутри.
│   ├── browser/                 # модули для работы с браузером, метамаском
│   │   ├── ads.py               # класс для работы с браузером ADS и библиотекой playwright.
│   │   ├── metamask.py          # класс для работы с метамаском используя объект ADS.
│   ├── dapps/                   # модуль для работы с децентрализованными приложениями. (в разработке)
│   ├── exchanges/               # модуль для работы с биржами.
│   │   ├── abs_exchange.py      # интерфейс для работы с биржами, абстрактный класс, от которого наследуются все биржи.
│   │   ├── exchanges.py         # класс для работы с биржами, инициализация всех бирж.
│   │   ├── okx.py               # класс для работы с биржей OKX.
│   │   ├── binance.py           # класс для работы с биржей Binance.
│   ├── onchain.py               # модуль для работы с блокчейном.
│   ├── excel.py                 # модуль для работы с excel файлами.
│   ├── bot.py                   # основной класс бота, который управляет всеми модулями.
│── logs/                        # логи работы скрипта.
│── models/                      # модели данных для работы скрипта. (заготовки для данных)
│   ├── chain.py                 # модель сети, хранилище данных сети.
│   ├── account.py               # модель аккаунта, хранилище данных аккаунта.
│   ├── contract_raw.py          # модель контракта, хранилище данных контракта.
│   ├── token.py                 # модель токена, хранилище данных токена, наследуется от contract_raw.
│   ├── exceptions.py            # уникальные исключения для проекта.
│   ├── withdraw.py              # модель вывода с биржи, хранилище данных вывода с валидацией.
│── snippets/                    # фрагменты готового кода для использования в проекте.
│── utils/                       # вспомогательные функции для работы скрипта.
│   ├── logging.py               # настройка логирования
│   ├── utils.py                 # вспомогательные функции для работы скрипта.
│── gitignore.py                 # файл для игнорирования файлов git.
│── requirements.txt             # файл с зависимостями, пакетами, библоиотеками для работы скрипта.
│── run.py                       # файл для запуска скрипта.
```

## 📦 Установка

### Требования к системе

- **Python:** версия 3.11 или выше
- **Операционная система:** Windows, macOS, Linux
- **ADS Power:** установленная и настроенная программа
- **Память:** минимум 4 GB RAM

**Внимание:** Установка браузера Chromium НЕ требуется. Проект использует браузеры из ADS Power через API.

### Шаг 1: Установка и настройка ADS Power

#### 1.1 Установка ADS Power

1. Скачайте ADS Power с официального сайта: https://www.adspower.com/
2. Установите программу и создайте аккаунт
3. Запустите ADS Power

**Внимание:** Для работы скрипта необходимо иметь плантый тариф для работы с API.

#### 1.2 Создание профилей

1. В ADS Power нажмите "Новый профиль"
2. Настройте параметры профиля (User Agent, отпечатки браузера)
3. Сохраните профиль и запомните его номер
4. Создайте столько профилей, сколько вам нужно

#### 1.3 Настройка API-ключа (важно!)

**Внимание:** Для работы скрипта необходимо либо настроить API-ключ, либо отключить защиту в ADS Power.

**Вариант 1: Отключить защиту API-ключа (рекомендуется для начинающих)**

1. Откройте ADS Power
2. Перейдите в API Settings
3. Отключите опцию "API Key Authentication" или "Security Verification"
4. Перезапустите ADS Power

**Вариант 2: Использовать API-ключ**

1. Откройте ADS Power
2. Перейдите в Settings (Настройки) → API Settings
3. Включите "API Key Authentication"
4. Скопируйте сгенерированный API-ключ
5. Откройте `config/settings.py`
6. Вставьте ключ в параметр `adspower_api_key`:

```python
adspower_api_key = 'ваш_api_ключ_здесь'
```

**Важно:** Без выполнения одного из этих шагов скрипт не сможет подключиться к ADS Power!

#### 1.4 Установка Metamask (опционально)

Если планируете работать с Metamask:

1. Откройте профиль в ADS Power
2. Установите расширение Metamask из Chrome Web Store
3. Скопируйте ID расширения из адресной строки (например: `chrome-extension://nkbihfbeogaeaoehlefnkodbefgpgknn/home.html`)
4. Вставьте ID в `config/settings.py` в параметр `metamask_url`

**Важно:** ADS Power должен быть запущен перед стартом скрипта!

### Шаг 2: Установка Python зависимостей

```bash
# Перейдите в директорию проекта
cd The-Template

# Установите зависимости
pip install -r requirements.txt
```

**Что устанавливается:**

- `playwright` - для управления браузером через ADS API
- `web3` - для работы с блокчейном
- `openpyxl` - для работы с Excel
- `loguru` - для логирования
- `requests` - для HTTP запросов
- `python-dotenv` - для загрузки .env
- `pyperclip` - для работы с буфером обмена

### Шаг 3: Настройка файлов конфигурации

#### 3.1 Настройка .env файла (для бирж и Telegram)

```bash
# Переименуйте файл-пример
cd config
cp .env.example .env # переименуйте файл с примерами, удалите .example
```

Откройте `config/.env` и заполните нужные данные:

```env
# Для работы с OKX (опционально, только если нужен вывод с биржи)
OKX_API_KEY_MAIN=your_okx_api_key
OKX_SECRET_KEY_MAIN=your_okx_secret_key
OKX_PASSPHRASE_MAIN=your_okx_passphrase

# Для работы с Binance (опционально, только если нужен вывод с биржи)
BINANCE_API_KEY=your_binance_api_key
BINANCE_SECRET_KEY=your_binance_secret_key

# Для уведомлений в Telegram (опционально)
BOT_TOKEN=your_telegram_bot_token
```

**Как получить API ключи:**

- **OKX:** https://www.okx.com/account/my-api (Разрешения: Read + Withdraw)
- **Binance:** https://www.binance.com/en/my/settings/api-management (Разрешения: Read + Withdraw)
- **Telegram Bot:** Напишите @BotFather в Telegram → `/newbot`
- **Chat ID:** Напишите @getmyid_bot в Telegram

#### 3.2 Настройка файлов с данными

Переименуйте файлы-примеры в директории `config/data/`:

```bash
cd config/data

# Рекомендуется (для удобства)
mv accounts.xlsx.example accounts.xlsx # переименуйте файл с примерами, удалите .example

# или если не используете Excel, то переименуйте текстовый файл с номерами профилей
mv profile_numbers.txt.example profile_numbers.txt # переименуйте файл с примерами, удалите .example


# Опциональные файлы (переименовывайте по необходимости)
mv private_keys.txt.example private_keys.txt # переименуйте файл с примерами, удалите .example
mv passwords.txt.example passwords.txt # переименуйте файл с примерами, удалите .example
mv seeds.txt.example seeds.txt # переименуйте файл с примерами, удалите .example
mv proxies.txt.example proxies.txt # переименуйте файл с примерами, удалите .example
mv addresses.txt.example addresses.txt # переименуйте файл с примерами, удалите .example
```

#### 3.3 Заполнение данных

**Вариант 1: Через Excel (рекомендуется)**

Откройте `config/data/accounts.xlsx` и заполните таблицу:

| Profile Number | Address  | Password  | Private Key | Seed         | Proxy             |
| -------------- | -------- | --------- | ----------- | ------------ | ----------------- |
| 12345          | 0x123... | MyPass123 | 0xabc...    | word word... | ip:port:user:pass |
| 12346          | 0x456... | MyPass456 | 0xdef...    | word word... | ip:port:user:pass |

**Минимально необходимо:** Profile Number, остальные поля можно оставить пустыми.

**Опциональные поля:**

- **Address** - заполняется автоматически из Private Key (можно оставить пустым)
- **Password** - нужен только для авторизации в кошельке (например, Metamask)
- **Private Key** - нужен для отправки транзакций через модуль Onchain, напрямую в блокчейн без использования браузера.
- **Seed** - нужен для импорта кошелька в расширение кошелька (например, Metamask)
- **Proxy** - формат `ip:port:login:password`

**Вариант 2: Через текстовые файлы (если не используете Excel)**

Заполните файлы построчно:

`profile_numbers.txt`:

```
12345
12346
12347
```

`private_keys.txt` (если нужна работа с блокчейном):

```
0xabc123...
0xdef456...
0xghi789...
```

**Важно!** В `config/settings.py` обязательно установите:

```python
accounts_source = 'txt'  # ОБЯЗАТЕЛЬНО измените с 'excel' на 'txt'!
```

### Шаг 4: Настройка config/settings.py

Откройте `config/settings.py` и настройте основные параметры:

```python
# Запуск браузера (обязательно True для работы с ADS)
is_browser_run = True

# Источник данных
accounts_source = 'excel'  # или 'txt'

# Скорость работы бота (пауза между действиями в мс)
speed = [800, 1200]  # увеличьте если элементы не успевают загружаться

# Настройка циклов
cycle = 1  # сколько раз прогнать все профили
pause_between_profile = [5, 10]  # пауза между профилями в секундах
pause_between_cycle = [300, 600]  # пауза между циклами в секундах

# Случайный порядок профилей
is_random = False

# Использование расписания и фильтрации
is_schedule = False

# Прокси
set_proxy = False  # установить прокси в профили ADS
check_proxy = False  # проверять прокси на работоспособность
is_mobile_proxy = False  # используются мобильные прокси

# Настройки блокчейна
start_chain = Chains.ARBITRUM_ONE  # стартовая сеть
is_web3_proxy = False  # использовать прокси для RPC запросов
gas_price_limit = 60  # лимит газа в gwei

# Telegram уведомления
chat_id = '12345678'  # ваш Telegram chat ID
alert_types = ['CRITICAL', 'SUCCESS']  # типы уведомлений

# URL Metamask расширения (получите из адресной строки)
metamask_url = 'chrome-extension://nkbihfbeogaeaoehlefnkodbefgpgknn/home.html'
```

### Шаг 5: Проверка установки

Запустите тестовый скрипт:

```bash
python run.py
```

**Что должно произойти:**

1. ✅ В консоли появятся логи инициализации
2. ✅ ADS Power откроет первый профиль
3. ✅ Браузер перейдет на Google.com
4. ✅ После задержки профиль закроется
5. ✅ Процесс повторится для следующих профилей

**Возможные проблемы и решения:**

| Ошибка                | Причина                    | Решение                                |
| --------------------- | -------------------------- | -------------------------------------- |
| `Connection refused`  | ADS Power не запущен       | Запустите ADS Power                    |
| `Profile not found`   | Неверный номер профиля     | Проверьте номера в ADS Power           |
| `ModuleNotFoundError` | Не установлены зависимости | `pip install -r requirements.txt`      |
| `Permission denied`   | Проблема с файлами         | Проверьте права доступа к папке        |
| `Metamask not found`  | Неверный URL расширения    | Проверьте `metamask_url` в settings.py |

### Шаг 6: Первая настройка Metamask (опционально)

Если планируете работать с Metamask, добавьте в функцию `activity()` в файле `run.py`:

```python
def activity(bot: Bot):
    # Создать новый кошелек
    password = "YourSecurePassword123!"
    bot.metamask.create_wallet(password=password)
    logger.success(f"Кошелек создан с паролем: {password}")

    # Или импортировать существующий
    bot.metamask.import_wallet(
        seed=bot.account.seed,
        password=bot.account.password
    )
    logger.success("Кошелек импортирован")
```

**Готово!** Теперь вы можете начать создавать свои скрипты автоматизации.

## Настройка:

Все настройки скрипта, все изменяемые данные загружаемые в скрипт, должны загружаться через файлы в директории
`config`.

### Загрузка данных в скрипт:

Для загрузки данных в скрипт необходимо заполнить файлы в директории `config/data`.

- У всех файлов, которые имеют расширение .example, необходимо переименовать их, убрав .example. (они попадут в игнор лист git)
- Данные в скрипт можно загрузить либо из файла `accounts.xlsx`, либо из текстовых файлов в директории `config/data`.
- Предпочтительным способом является загрузка данных из файла `accounts.xlsx`.
- По стандарту при использовании модуля Excel данные будут записываться в таблицу `config/data/accounts.xlsx`.
- Если необходима работа с другими таблицами, помещайте их в папку `config/data`, скрипт будет искать таблицу в данной директории.
- Если при использовании модуля Excel указать несуществующую таблицу, она создастся в папке `config/data`.
- Если у вас нет Microsoft Excel, то можно использовать бесплатные аналоги:
  - LibreOffice Calc для Windows, Linux и macOS - https://www.libreoffice.org/
  - OpenOffice Calc для Windows, Linux и macOS - https://www.openoffice.org/
  - Numbers для macOS - https://apps.apple.com/us/app/numbers/id409203825?mt=12 (НЕ РЕКОМЕНДУЕТСЯ)
- Способ загрузки данных в скрипт указывается в файле `config/settings.py` в переменной `accounts_source`, установите значение `excel` или `txt`.
- Для работы скрипта минимально необходимо заполнить номера профилей.
- Если вы укажите 100 номеров профилей и 150 паролей от метамаска, скрипт возьмет первые 100 профилей и
  первые 100 паролей. (длина рабочих данных определяется по количеству номеров профилей).
- Если вам не нужно авторизоваться в метамаске, пароли от метамаска можно не указывать.
- Если вам не нужно отправлять транзакции в блокчейн при помощи модуля Onchain, приватные ключи можно не указывать.
- Если вам нужно работать с балансом кошелька в профиле и выводом с биржи, заполните адреса кошельков.
- Если указать приватные ключи, но не указывать адреса кошельков, скрипт самостоятельно определит адреса кошельков
  по приватным ключам.
- Если вам нужно установить прокси в профили ADS укажите прокси в таблице. Формат `ip:port:login:password`
- Если у вас уже настроены прокси и их не нужно устанавливать, можно не указывать прокси.
- Если вам не нужно устанавливать прокси, но вы хотите проверять их на работоспособность и верность определяемого
  ip адреса, укажите прокси. Формат `ip:port:login:password`

### .env - загрузка приватных данных

Приватные данные для работы скрипта указываются в файле `config/.env`:

- удалите расширение `.example` из названия файла `config/.env.example`, чтобы скрипт мог загрузить приватные данные.
- `OKX_API_KEY_MAIN` - ключ API для работы с биржей OKX.
- `OKX_SECRET_KEY_MAIN` - секретный ключ API для работы с биржей OKX.
- `OKX_PASSPHRASE_MAIN` - пароль для работы с биржей OKX.
- `BINANCE_API_KEY` - ключ API для работы с биржей Binance.
- `BINANCE_SECRET_KEY` - секретный ключ API для работы с биржей Binance.
- `BOT_TOKEN` - токен бота для отправки уведомлений в телеграм. (можно получить в телеграм у @BotFather)

### Настройки в `config/settings.py`

Настройка работы скрипта делается в файле `config/settings.py`:

- `speed` - скорость работы робота в браузере, пауза между действиями в миллисекундах (от и до),
  увеличивайте если робот работает слишком быстро и элементы не успевают загружаться.
- `accounts_source` - источник данных для работы скрипта (excel или txt).
- `is_browser_run` - `True` или `False`, запускать браузер или нет (например работа с балансами не требует запуска браузера).
  Eсли `False`, то не будет работать модуль ads, metamask, будет выходить ошибка.
- `date_format` - формат даты для записи в excel файл, при использовании методов работы с датами. (модуль datetime)
- `is_random` - `True` или `False`, случайный порядок выбора и запуска профилей.
- `is_schedule` - `True` или `False`, включать ли расписание и фильтрацию аккаунтов, которое настраивается в файле run в функции schedule_and_filter.
- `pause_between_profile` - пауза между запуском профилей в секундах, от и до.
- `cycle` - количество циклов работы скрипта (проходов по всем профилям).
- `pause_between_cycle` - пауза между каждой итерации цикла в секундах, от и до.
- `okx_proxy` - прокси для работы с биржей OKX, для защиты API по ip адресу или если вы находитесь в стране, где заблокирована биржа. (например РФ). Формат `ip:port:login:password`
- `binance_proxy` - прокси для работы с биржей Binance, для защиты API по ip адресу или если вы находитесь в стране, где заблокирована биржа. (например РФ). Формат `ip:port:login:password`
- `set_proxy` - `True` или `False`, устанавливать прокси в профили или нет. Можно использовать даже если `is_browser_run = False`.
- `check_proxy` - `True` или `False`, проверять прокси на работоспособность или нет.
  сравнивает ip адреса прокси с ip адресом, который определяет скрипт. Работает только с `is_browser_run = True`
- `is_mobile_proxy` - `True` или `False`, используются ли мобильные прокси.
- `link_change_ip` - ссылка для смены ip адреса при использовании мобильных прокси.
- `start_chain` - стартовая сеть для работы скрипта в блокчейне. (не относится к метамаску)
- `is_web3_proxy` - `True` или `False`, использовать прокси для работы с блокчейном. Прокси будут браться из файла `config/data/proxies.txt` или из файла `config/data/accounts.xlsx`.
- `gas_price_limit` - лимит цены газа для метода `Onchain.gas_price_wait`, ожидающий газ ниже указанного лимита в gwei.
- `chat_id` - id вашего аккаунта в телеграм, чтобы бот мог отправлять вам уведомления. (можно получить в боте @getmyid_bot)
- `alert_types` - тип логов, по которым необходимо отправлять уведомления в телеграм, возможные варианты:
  - "CRITICAL" - при выводе лога logger.critical()
  - "SUCCESS" - при выводе лога logger.success()
  - "ERROR" - при выводе лога logger.error()
  - "WARNING" - при выводе лога logger.warning()
  - "INFO" - при выводе лога logger.info()
  - "DEBUG" - при выводе лога logger.debug()
- `metamask_url` - адрес расширения метамаск, можно получить в адресной строке при открытии расширения на весь экран.
- `adspower_api_key` - API-ключ для работы с ADS Power (если включена защита по API-ключу). Получить можно в настройках ADS Power → API Settings. Если защита выключена, оставьте пустым.

### Telegram уведомления

Система уведомлений позволяет отправлять сообщения в Telegram при определенных событиях. Для работы необходимо:
1. Создать бота через @BotFather и получить `BOT_TOKEN`
2. Узнать свой `chat_id` через @getmyid_bot
3. Добавить токен в `.env` и `chat_id` в `config/settings.py`

#### Способы отправки уведомлений

#### Способ 1: Автоматическая отправка по уровню лога

Настройте `alert_types` в `config/settings.py`:

```python
alert_types = ['CRITICAL', 'SUCCESS']
```

Теперь все логи этих уровней автоматически отправятся в Telegram:

```python
logger.success("Все транзакции выполнены!")  # Отправится в Telegram
logger.critical("Закончились средства!")      # Отправится в Telegram
logger.info("Проверка баланса...")            # НЕ отправится
```

**Доступные уровни для alert_types:**

- `"DEBUG"`
- `"INFO"`
- `"SUCCESS"`
- `"WARNING"`
- `"ERROR"`
- `"CRITICAL"`

#### Способ 2: Явная отправка через функцию

Используйте функцию `send_telegram_message()`:

```python
from utils.utils import send_telegram_message

def activity(bot: Bot):
    balance = bot.onchain.get_balance()

    if balance.ether > 1:
        # Отправить уведомление, но не писать в лог
        send_telegram_message(f"💰 Большой баланс: {balance.ether} ETH")
```

**Когда использовать:**

- Нужно отправить уведомление БЕЗ записи в лог
- Нужно отправить сообщение вне зависимости от `alert_types`

#### Способ 3: Отправка с записью в лог через bind

Используйте `.bind(telegram=True)`:

```python
def activity(bot: Bot):
    balance = bot.onchain.get_balance()

    # Записать INFO лог и отправить в Telegram
    logger.bind(telegram=True).info(f"Баланс проверен: {balance.ether} ETH")
```

**Когда использовать:**

- Нужно одновременно записать лог И отправить в Telegram
- Уровень лога не входит в `alert_types`

#### Практические примеры с Telegram

**Пример 1: Уведомление о завершении работы**

```python
def activity(bot: Bot):
    # Выполнение операций
    bot.onchain.send_token(to_address='0x...', amount=0.01)
    bot.excel.increase_counter('TX Count')

    # Автоматически отправится в Telegram (если SUCCESS в alert_types)
    logger.success(f"✅ Аккаунт {bot.account.profile_number} завершил работу")
```

**Пример 2: Критические ошибки**

```python
def activity(bot: Bot):
    balance = bot.onchain.get_balance()

    if balance.ether < 0.001:
        # Отправится в Telegram (если CRITICAL в alert_types)
        logger.critical(f"🚨 Критически низкий баланс: {balance.ether} ETH на аккаунте {bot.account.address}")
        return
```

**Пример 3: Еженедельная статистика**

```python
from datetime import datetime

def send_weekly_report():
    excel = Excel(file='accounts.xlsx')

    # Получить статистику
    all_swaps = excel.get_counters('Swaps')
    total_swaps = sum(all_swaps)

    # Создать отчет
    report = f"""
📊 Еженедельный отчет
📅 Дата: {datetime.now().strftime('%Y-%m-%d')}
🔄 Всего свопов: {total_swaps}
👥 Активных аккаунтов: {len(all_swaps)}
📈 Среднее на аккаунт: {total_swaps / len(all_swaps):.1f}
    """

    # Отправить в Telegram
    send_telegram_message(report)
```

**Пример 4: Использование эмодзи**

```python
# Telegram поддерживает эмодзи
logger.bind(telegram=True).success("✅ Транзакция подтверждена")
logger.bind(telegram=True).warning("⚠️ Низкий газ")
send_telegram_message("💰 Вывод с биржи завершен")
```

#### Настройка уровней для Telegram

**Минимальные уведомления (только критичное):**

```python
alert_types = ['CRITICAL']
```

**Важные события:**

```python
alert_types = ['CRITICAL', 'ERROR']
```

**Успехи и проблемы:**

```python
alert_types = ['CRITICAL', 'SUCCESS']
```

**Все важное:**

```python
alert_types = ['CRITICAL', 'ERROR', 'WARNING', 'SUCCESS']
```

**Вообще все (не рекомендуется):**

```python
alert_types = ['DEBUG', 'INFO', 'WARNING', 'ERROR', 'CRITICAL', 'SUCCESS']
```

### Chains - хранилище сетей для работы скрипта

В файле `chains.py` содержится класс `Chains` (документация ниже), который используется для хранения объектов модели `Chain`, т.е. объекты
сетей, которые используются для работы модулей `Metamask` и `Onchain`.

- Класс `Chains` импортирован в файл `run.py`, вызывая класс по названию `Chains`, через точку можно получить доступ ко всем объектам сетей.
- Информацию для заполнения объектов сетей можно получить с сайтов https://chainlist.org/, https://chainid.network/chains.json и https://api.debank.com/chain/list
- Название переменных для хранения объектов сетей пишите в большом регистре, название точно должно отражать название и тип сети, например: `ETHEREUM`, `ARBITRUM_ONE`.
- Название переменных для хранения объектов сетей должно быть уникальным, не должно повторяться.
- Название переменных для хранения объектов сетей должно совпадать с названием сети в параметре `name` у объекта сети, без учета регистра.
- Если есть 2 сети одна Mainnet другая Testnet, то название переменных для хранения объектов сетей должно быть одинаковым, но вторая сеть должна иметь в конце названия `_TESTNET`, так же добавьте слово `testnet` в `name` и `metamask_name`.
- Для добавления сетей в класс `Chains` используется модель `Chain`, класс `Chain` располагается в файле `models/chain.py`. Описание класса будет ниже.
- Метод класса `get_chain(cls, name: str) -> Chain`, который позволяет найти нужный объект сети по названию, ищется полное соответствие без учета регистра,
- Метод класса `get_chains_list(cls) -> list[Chain]`, который позволяет получить список всех объектов сетей из класса `Chains`. Удобно для перебора всех сетей.

#### Примеры использования:

- в `config/settings.py` в параметре `start_chain` можно указать стартовую сеть для работы скрипта: `start_chain = Chains.ETHEREUM`
- если необходимо выбрать сеть в метамаске, в метод `select_chain`, можно передать объект сети из класса `Chains`: `bot.metamask.select_chain(Chains.ETHEREUM)`
- если необходимо добавить сеть в метамаск, в метод `set_chain`, можно передать объект сети из класса `Chains`: `bot.metamask.set_chain(Chains.ETHEREUM)`
- при создании объектов `Token`, в конфигурации токенов в файле `config/tokens.py`, сеть токена указывается через объект сети из класса `Chains`: `chain = Chains.ETHEREUM`
- при создании объектов `Contract`, в конфигурации контрактов в файле `config/contracts.py`, сеть контракта указывается через объект сети из класса `Chains`: `chain = Chains.ETHEREUM`
- при поиске нужного токена в `Tokens`, в методе `get_token_by_symbol`, можно передать объект сети из класса `Tokens.get_by_symbol('USDT', Chains.ETHEREUM)`
- при создании объекта подключения к блокчейну через класс Onchain, в параметре `chain` можно передать объект сети из класса `onchain_ethereum = Onchain(account, Chains.ETHEREUM)`
- можно получить список всех объектов сетей из класса `Chains` через метод класса `get_chains_list`: `chains = Chains.get_chains_list()`, далее можно перебрать все сети в цикле,
  создавать объекты onchain, для работы с несколькими сетями:
- при выводе токенов с бирж okx или binance можно передать объект сети из класса `Chains`: `bot.okx.withdraw(Tokens.USDT_ETHEREUM, Chains.ETHEREUM)`,
  чтобы не запоминать названия всех сетей на всех биржах

```python
# получаем список сетей
chains = Chains.get_chains_list()
for chain in chains:
    # создаем объект onchain для работы с балансами
    onchain = Onchain(bot.account, chain)
    # получаем баланс нативного токена сети
    onchain.get_balance()
```

### Сhain - модель сети, хранилище данных сети

Модель Chain, используется для создания объектов сети, класс `Chain` располагается в файле `models/chain.py`.
Модель Chain, используется для заполнения объектов сетей в классе `Chains`, который хранит все объекты сетей для работы скрипта.
Модель Chain, используется в аннотациях методов и функций, где необходимо указать сеть для работы.
Объект Chain, используется везде где необходимо взаимодействие с сетью:

- в модуле `Metamask` для выбора сети, добавления сети.
- в модуле `Onchain` для создания объекта подключения к блокчейну.
- в модуле `Tokens` для создания объектов токенов.
- в модуле `Contracts` для создания объектов контрактов.

**При создании объекта Chain необходимо указать следующие следующие параметры:**

- `name` - название сети, желательно использовать название сети совпадающее с названием переменной в классе `Chains`.
- `rpc` - rpc адрес сети, можно получить с сайтов https://chainlist.org/, https://chainid.network/chains.json и
- https://api.debank.com/chain/list, данный адрес rpc ноды будет использоваться для добавления сети в метамаске, а так же для подключения к блокчейну
  в модуле Onchain (отправка транзакций и получения балансов)
- `chain_id` - id сети можно получить с сайтов https://chainlist.org/, https://chainid.network/chains.json и
- https://api.debank.com/chain/list, данный id сети будет использоваться для добавления сети в метамаске, а так же для подключения к блокчейну
  в модуле Onchain (отправка транзакций и получения балансов)
- `metamask_name` - название сети в метамаске, будет использоваться при добавлении сети в метамаск, а так же
  для выбора. (по стандарту берется из параметра name).
- `native_token` - название нативного токена сети, по стандарту стоит `ETH`, нужно для работы с балансами.
  А так же для ведения логов.
- `is_eip1559` - если сеть поддерживает EIP-1559, то True, иначе False, можно взять тут https://api.debank.com/chain/list , если не заполнить,
  скрипт определит сам, но будет отправлять лишний запрос на rpc ноду.
- `okx_name` - название сети в бирже OKX, корректный список названий сетей можно получить
  вызвав метод `bot.exchanges.okx.get_chains()` в скрипте, если указать некорректно перестанет работать вывод с биржи.
- `binance_name` - название сети в бирже binance, корректный список названий сетей можно получить
  вызвав метод `bot.exchanges.binance.get_chains()` в скрипте, если указать некорректно перестанет работать вывод с биржи.

Обязательными параметрами являются `name`, `rpc` и `chain_id`, но настоятельно рекомендуется заполнять все поля.

### Tokens - хранилище токенов для работы скрипта

В файле `tokens.py` содержится класс `Tokens`, который используется для хранения объектов модели `Token`, т.е. объекты
токенов, которые используются для работы модуля `Onchain`, содержат все все необходимые данные описываюшие
токенов в различных блочейнах.

- Класс `Tokens` импортирован в файл `run.py`, вызывая класс по названию `Tokens`, через точку можно получить доступ ко всем объектам токенов.
- Адреса смарт-контрактов токенов в нужной сети можно получить с сайтов https://dropstab.com, http://coinmarketcap.com, https://coingecko.com
- Информацию для заполнения объектов токенов можно получить в эксплорерах блокчейнов (ищите по адресу контракта):
  - токены в сети Ethereum можно получить с сайта https://etherscan.io/tokens
  - токены в сети Binance Smart Chain можно получить с сайта https://bscscan.com/tokens
  - токены в сети Arbitrum One можно получить с сайта https://arbiscan.io/tokens
  - и т.д. Практически у всех популярных блокчейнов есть свои эксплореры от etherscan.io.
- Название переменных для хранения объектов токенов пишите в большом регистре, название должно состоять из
  официального тикера токена и сети, например: `USDT_ETHEREUM`, `USDT_ARBITRUM_ONE`.
- Создавайте объекты токенов только для ERC20 токенов, создавать объекты нативных токенов (ETH, BNB, MATIC) различных блокчейнов не нужно.
- Названия переменных для хранения объектов токенов должно быть уникальным, не должно повторяться.
- Для добавления токенов в класс `Tokens` используется модель `Token`, класс `Token` располагается в файле `models/token.py`. Описание класса будет ниже.
- Метод класса `get_token_by_address(cls, address: str) -> Token`, который позволяет
  найти нужный объект токена по адресу контракта, ищется полное соответствие, без учета регистра.
- Метод класса `get_token_by_symbol(cls, symbol: str, chain: Chain) -> Token`,
  который позволяет найти нужный объект токена по символу токена и сети, ищется полное соответствие, без учета регистра.
- Метод класса `get_tokens_by_chain(cls, chain: Chain) -> list[Token]`, который позволяет
  получить список всех объектов токенов из класса `Tokens` для определенной сети.
- Метод класса `get_tokens(cls) -> list[Token]`, который позволяет получить список всех объектов токенов из класса `Tokens`.
- Метод класс `add_token(cls, token: Token)`, который позволяет добавить объект токена в класс `Tokens` на время работы скрипта.

#### Примеры использования:

- можно передать объект токена в метод `send_token`, для отправки токена: `bot.onchain.send_token(Tokens.USDT_ETHEREUM, '0x...', 0.1)`
- можно передать объект токена в метод `get_balance`, для получения баланса токена: `bot.onchain.get_balance(Tokens.USDT_ETHEREUM)`
- можно найти объект токена по адресу контракта: `token = Tokens.get_token_by_address('0x...')`
- можно найти объект токена по символу токена и сети: `token = Tokens.get_token_by_symbol('USDT', Chains.ETHEREUM)`
- можно получить список всех объектов токенов из класса `Tokens`: `tokens = Tokens.get_tokens()` и далее перебрать все токены в цикле.
- можно получить список всех объектов токенов для определенной сети: `tokens = Tokens.get_tokens_by_chain(Chains.ETHEREUM)` и далее перебрать все токены в цикле.

```python
# получаем список токенов для сети Ethereum, проверяем баланс и отправляем токены
tokens = Tokens.get_tokens_by_chain(Chains.ETHEREUM)
for token in tokens:
    # проверяем баланс токена
    balance = bot.onchain.get_balance(token=token)
    print(f'Баланс токена {token.symbol} = {balance}')
    # отправляем токен
    bot.onchain.send_token(token, '0x...', balance)
```

```python
# проверяем балансы всех токенов во всех сетях
chains = Chains.get_chains_list()
for chain in chains:
    chain_instance = Onchain(bot.account, chain)
    native_balance = chain_instance.get_balance()
    print(f'Баланс токена {chain.native_token} = {native_balance}')
    tokens = Tokens.get_tokens_by_chain(chain)
    for token in tokens:
        # проверяем баланс токена
        balance = chain_instance.get_balance(token=token)
        print(f'Баланс токена {token.symbol} = {balance}')

```

### Amount - модель для работы с суммами токенов

Класс `Amount` используется для корректной работы с криптовалютными суммами, решая проблему точности при конвертации между wei (минимальная единица) и обычными единицами (ether).

#### Зачем нужен Amount?

В блокчейне все суммы хранятся в минимальных единицах (wei). Например:

- 1 ETH = 1,000,000,000,000,000,000 wei (10^18)
- 1 USDT = 1,000,000 wei (10^6, так как у USDT 6 decimals)

При работе с обычными числами (float) возникают проблемы с точностью. Класс `Amount` решает эту проблему, предоставляя удобный интерфейс для работы с суммами.

#### Создание объекта Amount

```python
from models.amount import Amount

# Создание из обычного числа (ether)
amount1 = Amount(1.5)  # 1.5 ETH
amount2 = Amount(10, decimals=6)  # 10 USDT (6 decimals)

# Создание из wei
amount3 = Amount(1500000000000000000, wei=True)  # 1.5 ETH в wei
amount4 = Amount(10000000, decimals=6, wei=True)  # 10 USDT в wei
```

#### Атрибуты класса Amount

```python
amount = Amount(1.5)

# Доступ к различным представлениям суммы
print(amount.wei)            # 1500000000000000000 (int) - для транзакций
print(amount.ether)          # 1.5 (float) - для обычных операций
print(amount.ether_decimal)  # Decimal('1.5') - для точных вычислений
print(amount.decimals)       # 18 - количество знаков после запятой
```

**Когда использовать каждый атрибут:**

- `wei` - для отправки в транзакции блокчейна
- `ether` - для обычных операций, логирования, отображения
- `ether_decimal` - для точных финансовых расчетов

#### Математические операции

Класс `Amount` поддерживает все основные математические операции:

```python
# Сложение
amount1 = Amount(1.5)
amount2 = Amount(2.3)
result = amount1 + amount2  # Amount(3.8)

# Можно складывать с обычными числами
result = amount1 + 0.5  # Amount(2.0)

# Вычитание
result = amount2 - amount1  # Amount(0.8)
result = amount1 - 0.3      # Amount(1.2)

# Умножение
result = amount1 * 2        # Amount(3.0)
result = amount1 * amount2  # Amount(3.45)

# Деление
result = amount1 / 2        # Amount(0.75)
result = amount1 / amount2  # Amount(0.652...)

# Целочисленное деление
result = amount1 // 0.5     # Amount(3.0)

# Остаток от деления
result = amount1 % 0.7      # Amount(0.1)

# Возведение в степень
result = amount1 ** 2       # Amount(2.25)
```

#### Операции сравнения

```python
amount1 = Amount(1.5)
amount2 = Amount(2.0)

# Все стандартные операции сравнения
amount1 == 1.5      # True
amount1 != 2.0      # True
amount1 < amount2   # True
amount1 <= 1.5      # True
amount1 > 1.0       # True
amount1 >= 1.5      # True
```

#### Практические примеры использования

**Пример 1: Получение и отображение баланса**

```python
# Модуль Onchain возвращает Amount
balance = bot.onchain.get_balance()

# Удобное отображение
logger.info(f'Баланс: {balance.ether} ETH')  # Баланс: 1.5 ETH

# Для транзакции используем wei
if balance.wei > 0:
    # Отправить половину баланса
    amount_to_send = balance / 2
    bot.onchain.send_token(amount=amount_to_send.ether, to_address='0x...')
```

**Пример 2: Расчет комиссии**

```python
balance = bot.onchain.get_balance()
fee = Amount(0.001)  # Комиссия сети

# Рассчитать сумму для отправки (баланс минус комиссия)
amount_to_send = balance - fee

if amount_to_send.ether > 0:
    logger.info(f'Отправляем: {amount_to_send.ether} ETH')
    bot.onchain.send_token(amount=amount_to_send.ether, to_address='0x...')
```

**Пример 3: Работа с токенами с разными decimals**

```python
# USDT имеет 6 decimals
usdt_balance = bot.onchain.get_balance(token=Tokens.USDT_ARBITRUM_ONE)
logger.info(f'USDT: {usdt_balance.ether}')  # Автоматически учитывает decimals

# ETH имеет 18 decimals
eth_balance = bot.onchain.get_balance()
logger.info(f'ETH: {eth_balance.ether}')

# Нельзя складывать токены с разными decimals (будет ошибка)
# total = usdt_balance + eth_balance  # ValueError!
```

**Пример 4: Проверка достаточности баланса**

```python
balance = bot.onchain.get_balance()
required_amount = Amount(0.1)

if balance >= required_amount:
    logger.success('Баланс достаточен')
    # Отправить 80% от требуемой суммы
    amount_to_send = required_amount * 0.8
    bot.onchain.send_token(amount=amount_to_send.ether, to_address='0x...')
else:
    logger.error(f'Недостаточно средств. Есть: {balance.ether}, нужно: {required_amount.ether}')
```

**Пример 5: Точные вычисления с Decimal**

```python
from decimal import Decimal

balance = bot.onchain.get_balance()

# Для финансовых расчетов используйте ether_decimal
precise_amount = balance.ether_decimal * Decimal('0.333333')
logger.info(f'Точная сумма: {precise_amount}')

# Создать новый Amount из Decimal
new_amount = Amount(precise_amount)
```

**Пример 6: Распределение средств**

```python
balance = bot.onchain.get_balance()
recipients = ['0xabc...', '0xdef...', '0xghi...']

# Разделить баланс поровну между получателями
amount_per_recipient = balance / len(recipients)

for recipient in recipients:
    logger.info(f'Отправка {amount_per_recipient.ether} ETH на {recipient}')
    bot.onchain.send_token(amount=amount_per_recipient.ether, to_address=recipient)
```

#### Важные замечания

1. **Decimals должны совпадать**: При операциях между двумя `Amount` они должны иметь одинаковое количество decimals, иначе возникнет ошибка.

```python
amount1 = Amount(1.5, decimals=18)  # ETH
amount2 = Amount(100, decimals=6)   # USDT
# amount1 + amount2  # ValueError: разное количество знаков после запятой
```

2. **Wei всегда int**: Атрибут `wei` всегда возвращает целое число, так как в блокчейне нет дробных wei.

3. **Float неточен**: Для точных вычислений используйте `ether_decimal` вместо `ether`.

4. **Автоматическая конвертация**: Класс автоматически конвертирует между wei и ether, сохраняя точность.

#### Где используется Amount в проекте

- **Onchain.get_balance()** - возвращает Amount
- **Onchain.send_token()** - принимает amount как float или Amount
- Все операции с балансами и суммами токенов

#### Преимущества использования Amount

✅ Исключает ошибки конвертации между wei и ether
✅ Поддерживает математические операции
✅ Автоматически учитывает decimals токена
✅ Обеспечивает точность вычислений через Decimal
✅ Удобный интерфейс для работы

### Bot - центральный класс управления модулями

Класс `Bot` является главным классом проекта, который объединяет все модули и предоставляет единый интерфейс для работы с автоматизацией. Через объект bot можно получить доступ к всем модулям проекта, через точечную нотацию.

**Важно для студентов:** Вся ваша работа будет происходить в функции `activity(bot: Bot)` в файле `run.py`. Объект `bot` уже создан и передан вам как параметр функции. Все модули (`bot.ads`, `bot.metamask`, `bot.onchain`, `bot.excel`, `bot.exchanges`) уже инициализированы и готовы к использованию.

```python
def activity(bot: Bot):
    """
    Здесь вы пишете свой скрипт автоматизации.
    Объект bot уже создан и инициализирован.
    """
    # Используйте модули напрямую
    bot.ads.open_url('https://example.com')
    balance = bot.onchain.get_balance()
    bot.excel.set_cell('Status', 'Working')
```

#### Назначение и архитектура

`Bot` выступает как центральная точка управления, которая:

- Инициализирует все необходимые модули при создании
- Управляет жизненным циклом браузера и подключений
- Обеспечивает корректную обработку ошибок
- Предоставляет удобный доступ ко всем функциям проекта

**Архитектура:**

```
Bot (центральный контроллер)
├── ads         → Управление браузером ADS Power
├── metamask    → Работа с кошельком Metamask
├── onchain     → Работа с блокчейном (транзакции, балансы)
├── excel       → Работа с Excel таблицами (отчетность)
└── exchanges   → Работа с биржами (OKX, Binance)
    |___ okx     → Работа с биржей OKX
    |___ binance → Работа с биржей Binance

```

#### Инициализация бота

**Примечание:** В большинстве случаев вам не нужно самостоятельно создавать объект `Bot`. Он уже создается автоматически в функции `worker()` файла `run.py` и передается в вашу функцию `activity(bot)`.

**Сеть по умолчанию:** При инициализации `Bot` автоматически создает модуль `onchain`, подключенный к сети, указанной в `config.start_chain`:

```python
# В config/settings.py
start_chain = Chains.ARBITRUM_ONE  # Эта сеть будет использована для bot.onchain
```

**Ручная инициализация (для продвинутых случаев):**

```python
from core.bot import Bot
from models.account import Account
from config import Chains

# Создание аккаунта
account = Account(
    profile_number=12345,
    address='0x742d35Cc6634C0532925a3b844Bc9e7595f0bEb5',
    private_key='0xabc...',
    password='MyPassword123'
)

# Инициализация бота (использует start_chain из config)
bot = Bot(account)
# bot.onchain подключен к config.start_chain

# Или с явным указанием другой сети
bot = Bot(account, chain=Chains.ETHEREUM)
# bot.onchain подключен к Ethereum
```

**В вашем скрипте (функция activity):**

```python
def activity(bot: Bot):
    # bot уже инициализирован со всеми модулями
    # bot.onchain подключен к сети из config.start_chain

    logger.info(f'Работаем в сети: {bot.onchain.chain.name}')
    balance = bot.onchain.get_balance()
```

#### Использование как Context Manager (рекомендуется)

Класс `Bot` реализует протокол context manager, что обеспечивает:

- Автоматическое закрытие браузера
- Корректную обработку ошибок
- Логирование результата работы

```python
# Рекомендуемый способ использования
with Bot(account) as bot:
    # Ваша логика здесь
    bot.ads.open_url('https://google.com')
    balance = bot.onchain.get_balance()
    bot.excel.set_cell('Status', 'Success')
# Браузер автоматически закроется после блока with
```

**Что происходит автоматически:**

- При входе в контекст (`with`) - инициализация всех модулей
- При выходе из контекста - закрытие браузера и логирование результата
- При ошибке - корректная обработка и логирование

#### Доступные атрибуты (модули)

##### 1. bot.ads - Управление браузером

Модуль для работы с ADS Power браузером:

```python
with Bot(account) as bot:
    # Открыть URL
    bot.ads.open_url('https://app.uniswap.org')

    # Кликнуть по элементу
    bot.ads.click('button:has-text("Connect")')

    # Ввести текст
    bot.ads.type_text('input[placeholder="0.0"]', '100')

    # Подождать элемент
    bot.ads.wait_locator('text=Swap')

    # Получить текст
    text = bot.ads.get_text('div.balance')
```

##### 2. bot.metamask - Работа с кошельком

Модуль для взаимодействия с Metamask:

```python
with Bot(account) as bot:
    # Импортировать кошелек
    bot.metamask.import_wallet(
        seed=account.seed,
        password=account.password
    )

    # Авторизоваться
    bot.metamask.auth_metamask()

    # Переключить сеть
    bot.metamask.select_chain(Chains.ARBITRUM_ONE)

    # Подключиться к dApp
    bot.metamask.connect()

    # Подтвердить транзакцию
    bot.metamask.send_tx()

    # Подписать сообщение
    bot.metamask.sign()
```

##### 3. bot.onchain - Работа с блокчейном

Модуль для прямой работы с блокчейном (без браузера):

```python
with Bot(account) as bot:
    # Получить баланс нативного токена
    balance = bot.onchain.get_balance()
    logger.info(f'ETH: {balance.ether}')

    # Получить баланс ERC-20 токена
    usdt_balance = bot.onchain.get_balance(token=Tokens.USDT_ARBITRUM_ONE)

    # Отправить нативный токен
    bot.onchain.send_token(
        amount=0.01,
        to_address='0x742d35Cc6634C0532925a3b844Bc9e7595f0bEb5'
    )

    # Отправить ERC-20 токен
    bot.onchain.send_token(
        token=Tokens.USDT_ARBITRUM_ONE,
        amount=10,
        to_address='0x742d35Cc6634C0532925a3b844Bc9e7595f0bEb5'
    )

    # Approve токена
    bot.onchain.approve(
        token=Tokens.USDT_ARBITRUM_ONE,
        spender='0xcontractaddress...',
        amount=100
    )

    # Сменить сеть
    bot.onchain.change_chain(Chains.ETHEREUM)
```

##### 4. bot.excel - Работа с таблицами

Модуль для записи отчетности в Excel:

```python
with Bot(account) as bot:
    # Записать значение
    bot.excel.set_cell('Status', 'Working')

    # Прочитать значение
    status = bot.excel.get_cell('Status')

    # Увеличить счетчик
    bot.excel.increase_counter('TX Count')

    # Получить счетчик
    count = bot.excel.get_counter('TX Count')

    # Записать дату
    bot.excel.set_date('Last Activity')

    # Получить дату
    last_date = bot.excel.get_date('Last Activity')

    # Работа с другой таблицей
    bot.excel.change_table('report.xlsx', 'Sheet1')
```

##### 5. bot.exchanges - Работа с биржами

Модуль для вывода средств с бирж:

```python
with Bot(account) as bot:
    # Вывод с OKX
    bot.exchanges.okx.withdraw(
        token=Tokens.USDT_ARBITRUM_ONE,
        amount=50,
        chain=Chains.ARBITRUM_ONE,
        to_address=account.address
    )

    # Вывод с Binance
    bot.exchanges.binance.withdraw(
        token='ETH',
        amount=0.1,
        chain=Chains.ARBITRUM_ONE,
        to_address=account.address
    )

    # Получить список сетей
    okx_chains = bot.exchanges.okx.get_chains()
    binance_chains = bot.exchanges.binance.get_chains()
```

#### Обработка ошибок

Класс Bot автоматически обрабатывает ошибки и логирует результаты:

```python
with Bot(account) as bot:
    try:
        bot.ads.open_url('https://app.uniswap.org')
        balance = bot.onchain.get_balance()

        if balance.ether < 0.01:
            raise ValueError('Недостаточно средств')

        bot.onchain.send_token(amount=0.01, to_address='0x...')
        bot.excel.set_cell('Status', 'Success')

    except ValueError as e:
        logger.error(f'Ошибка валидации: {e}')
        bot.excel.set_cell('Status', 'Failed')
        bot.excel.set_cell('Error', str(e))
    except Exception as e:
        logger.critical(f'Критическая ошибка: {e}')
        bot.excel.set_cell('Status', 'Error')
```

**Автоматическая обработка специальных ошибок:**

- `TimeoutError` - логируется как ошибка таймаута
- `'object has no attribute: page'` - предупреждение о попытке работы с браузером когда `is_browser_run=False`
- Любые другие ошибки - логируются как критические

#### Паттерны использования

**Паттерн 1: Базовая автоматизация браузера**

```python
with Bot(account) as bot:
    bot.ads.open_url('https://example.com')
    bot.ads.click('button.connect')
    bot.metamask.connect()
    bot.excel.set_cell('Status', 'Connected')
```

**Паттерн 2: Работа только с блокчейном (без браузера)**

```python
# В config/settings.py: is_browser_run = False

with Bot(account) as bot:
    # Проверить балансы
    balance = bot.onchain.get_balance()

    # Отправить токены
    if balance.ether > 0.1:
        bot.onchain.send_token(amount=0.05, to_address='0x...')

    # Записать результат
    bot.excel.set_cell('Balance', balance.ether)
```

**Паттерн 3: Вывод с биржи + отправка**

```python
with Bot(account) as bot:
    # Вывести с биржи
    bot.exchanges.okx.withdraw(
        token=Tokens.USDT_ARBITRUM_ONE,
        amount=100,
        chain=Chains.ARBITRUM_ONE,
        to_address=account.address
    )

    # Подождать поступления
    random_sleep(300, 600)

    # Проверить баланс
    balance = bot.onchain.get_balance(token=Tokens.USDT_ARBITRUM_ONE)
    logger.info(f'Получено: {balance.ether} USDT')

    # Отправить на другой адрес
    bot.onchain.send_token(
        token=Tokens.USDT_ARBITRUM_ONE,
        amount=balance.ether * 0.95,
        to_address='0x...'
    )

    # Записать отчет
    bot.excel.set_cell('Status', 'Completed')
    bot.excel.set_date('Last TX')
```

**Паттерн 4: Проверка балансов во всех сетях**

```python
with Bot(account) as bot:
    chains = Chains.get_chains_list()

    for chain in chains:
        # Создать временный onchain для каждой сети
        chain_onchain = Onchain(account, chain)
        balance = chain_onchain.get_balance()

        logger.info(f'{chain.name}: {balance.ether} {chain.native_token}')
        bot.excel.set_cell(f'{chain.name} Balance', balance.ether)
```

**Паттерн 5: Множественные операции в браузере**

```python
with Bot(account) as bot:
    # Авторизация в Metamask
    if not bot.metamask.is_wallet_created():
        bot.metamask.import_wallet(account.seed, account.password)
    bot.metamask.auth_metamask()

    # Работа с dApp
    bot.ads.open_url('https://app.uniswap.org')
    bot.metamask.connect()

    # Выполнить swap через интерфейс
    bot.ads.type_text('input#input-currency', '100')
    bot.ads.click('button:has-text("Swap")')
    bot.metamask.send_tx()

    # Записать результат
    bot.excel.increase_counter('Swaps')
    bot.excel.set_date('Last Swap')
```

#### Важные замечания

1. **Всегда используйте context manager** (`with Bot(...)`) - это обеспечивает корректное закрытие ресурсов (по стандарту уже используется в заготовке)

2. **Проверяйте is_browser_run**: Если `is_browser_run=False`, модули `ads` и `metamask` будут недоступны

3. **Каждый Bot создается для одного аккаунта**: Для работы с множеством аккаунтов создавайте новый Bot в цикле

4. **Сеть можно менять**: Используйте `bot.onchain.change_chain()` для переключения между сетями

5. **Excel подключается автоматически**: Модуль excel автоматически находит строку с аккаунтом в таблице

#### Преимущества использования Bot

✅ Единая точка доступа ко всем модулям
✅ Автоматическое управление ресурсами
✅ Встроенная обработка ошибок
✅ Логирование всех операций
✅ Простой и понятный API
✅ Безопасное завершение работы

### Onchain - модуль для работы с блокчейном

Модуль `Onchain` предоставляет полный набор инструментов для работы с блокчейном без использования браузера. Он позволяет напрямую взаимодействовать с EVM-совместимыми сетями через RPC ноды.

#### Назначение модуля

Модуль Onchain используется для:

- Проверки балансов нативных и ERC-20 токенов
- Отправки токенов на другие адреса
- Одобрения (approve) токенов для смарт-контрактов
- Работы с кастомными контрактами
- Получения информации о газе
- Удаления неиспользуемых одобрений

**Преимущества:**

- ⚡ Быстрая работа без браузера
- 🔐 Безопасная подпись транзакций локально
- 🎯 Точный контроль над газом и комиссиями
- 🔄 Автоматическая работа с EIP-1559
- 💰 Оптимизация комиссий для экономии средств

#### Инициализация и подключение к сети

**Важно:** При работе с проектом, вы всегда будете писать свои скрипты в функции `activity()` в файле `run.py`, где бот уже инициализирован и передан как параметр. Модуль `onchain` доступен через `bot.onchain`.

**Сеть по умолчанию:** В конфиге `config/settings.py` указывается сеть, к которой будет подключен `bot.onchain` при инициализации:

```python
# В config/settings.py
start_chain = Chains.ARBITRUM_ONE  # Сеть по умолчанию для onchain
```

**Использование в функции activity():**

```python
def activity(bot: Bot):
    """Ваш скрипт автоматизации"""

    # bot.onchain уже подключен к сети из config.start_chain
    balance = bot.onchain.get_balance()
    logger.info(f'Баланс: {balance.ether} {bot.onchain.chain.native_token}')

    # Отправить токены
    bot.onchain.send_token(
        to_address='0x742d35Cc6634C0532925a3b844Bc9e7595f0bEb5',
        amount=0.01
    )

    # Переключить сеть при необходимости
    bot.onchain.change_chain(Chains.ETHEREUM)
    eth_balance = bot.onchain.get_balance()
```

**Ручная инициализация:**

```python
from core.onchain import Onchain
from models.account import Account
from config import Chains

# Вариант 1: Создание нового аккаунта с приватным ключом
account = Account(
    profile_number=12345,
    private_key='0xabc...'
)

# Инициализация подключения к конкретной сети
onchain = Onchain(account, Chains.ARBITRUM_ONE)
balance = onchain.get_balance()

# Вариант 2: Использование аккаунта из бота
# Создать дополнительный onchain для другой сети
# используя аккаунт из бота
ethereum_onchain = Onchain(bot.account, Chains.ETHEREUM)
eth_balance = ethereum_onchain.get_balance()

# Вариант 3: Массовая работа с множеством аккаунтов
from utils.utils import get_accounts

# Получить все аккаунты из файлов/excel
accounts = get_accounts()

# Быстро проверить балансы во всех аккаунтах
for account in accounts:
    onchain = Onchain(account, Chains.ARBITRUM_ONE)
    balance = onchain.get_balance()
    logger.info(f'Account {account.address}: {balance.ether} ETH')

# Или создать список onchain объектов для параллельной работы
onchain_list = [Onchain(acc, Chains.ARBITRUM_ONE) for acc in accounts]

# Выполнить операции для всех аккаунтов
for i, onchain in enumerate(onchain_list):
    balance = onchain.get_balance()
    if balance.ether > 0.01:
        onchain.send_token(
            to_address='0x742d35Cc6634C0532925a3b844Bc9e7595f0bEb5',
            amount=0.01
        )
        logger.success(f'Аккаунт {i+1}: отправлено 0.01 ETH')
```

#### Смена сети через change_chain()

### Как работает переключение между блокчейн сетями (change_chain)

Иногда возникает необходимость работать сразу с несколькими сетями (например, проверить балансы в разных сетях, отправить транзакции через разные блокчейны и т.д.). Для этих целей в модуле Onchain реализован удобный метод `change_chain()`, который позволяет мгновенно сменить сеть без переинициализации всего объекта.

**Что происходит при смене сети:**

- Меняется endpoint RPC ‒ теперь все операции выполняются в новой выбранной сети.
- Меняется сетевой параметр объекта (chain) внутри onchain ‒ используется корректная информация о нативном токене, fee и параметрах сети.
- Приватный ключ и остальные данные аккаунта сохраняются (адрес, proxy и т.д.), пересоздавать их не нужно.
- Можно безопасно вызывать любые методы: get_balance, send_token и др. — они будут работать с уже выбранной сетью.

**Типичные сценарии:**

- Сначала подключение к одной сети (например, Arbitrum) — операции только в ней.
- В любой момент можно переключиться на другую сеть (Ethereum, BSC, Optimism) вызвав `change_chain(Chains.ETHEREUM)` и продолжить работу.
- После смены сети можно мгновенно проверить баланс, отправить токен или сделать другой onchain-запрос именно в этой новой сети.

**Практический пример ниже показывает этот процесс:**

```python
# Инициализация с одной сетью
onchain = Onchain(account, Chains.ARBITRUM_ONE)

# Проверить баланс в Arbitrum
arb_balance = onchain.get_balance()
logger.info(f'Arbitrum: {arb_balance.ether} ETH')

# Переключиться на Ethereum
onchain.change_chain(Chains.ETHEREUM)

# Проверить баланс в Ethereum
eth_balance = onchain.get_balance()
logger.info(f'Ethereum: {eth_balance.ether} ETH')

# Переключиться на Optimism
onchain.change_chain(Chains.OPTIMISM)

# Отправить токен в новой сети
onchain.send_token(
    to_address='0x742d35Cc6634C0532925a3b844Bc9e7595f0bEb5',
    amount=0.01
)
```

**Параметры:**

- `chain`: Объект Chain новой сети

**Использование через Bot:**

```python
with Bot(account) as bot:
    # Текущая сеть из config.start_chain
    balance_arb = bot.onchain.get_balance()

    # Переключить сеть
    bot.onchain.change_chain(Chains.ETHEREUM)
    balance_eth = bot.onchain.get_balance()
```

**Альтернативный подход: несколько объектов Onchain одновременно**

Если нужно работать с несколькими сетями параллельно, можно создать отдельные объекты Onchain для каждой сети. Это удобно, когда требуется часто переключаться между сетями или выполнять операции в разных сетях одновременно:

```python
from core.onchain import Onchain

with Bot(account) as bot:
    # Использовать основной onchain из бота (сеть из config.start_chain)
    main_balance = bot.onchain.get_balance()

    # Создать дополнительные объекты для других сетей
    # Используем bot.account для доступа к данным аккаунта
    eth_onchain = Onchain(bot.account, Chains.ETHEREUM)
    bsc_onchain = Onchain(bot.account, Chains.BSC)
    optimism_onchain = Onchain(bot.account, Chains.OPTIMISM)

    # Теперь можно работать со всеми сетями одновременно
    eth_balance = eth_onchain.get_balance()
    bsc_balance = bsc_onchain.get_balance()
    op_balance = optimism_onchain.get_balance()

    logger.info(f'Ethereum: {eth_balance.ether} ETH')
    logger.info(f'BSC: {bsc_balance.ether} BNB')
    logger.info(f'Optimism: {op_balance.ether} ETH')

    # Отправить токены в разных сетях
    eth_onchain.send_token(to_address='0x...', amount=0.01)
    bsc_onchain.send_token(to_address='0x...', amount=0.01)
```

**Когда использовать каждый подход:**

- **change_chain()**: Когда работаете последовательно с разными сетями, переключаясь между ними по мере необходимости
- **Несколько объектов Onchain**: Когда нужно часто обращаться к нескольким сетям или выполнять параллельные операции

#### Метод get_balance() - проверка балансов

Получение баланса нативного токена или ERC-20:

```python
# Получить баланс нативного токена (ETH, BNB, MATIC и т.д.)
balance = bot.onchain.get_balance()
logger.info(f'Баланс: {balance.ether} ETH')

# Получить баланс ERC-20 токена, токен должен быть в той же сети, что и onchain
usdt_balance = bot.onchain.get_balance(token=Tokens.USDT_ARBITRUM_ONE)
logger.info(f'USDT: {usdt_balance.ether}')

# Получить баланс по адресу контракта токена
token_address = '0xFd086bC7CD5C481DCC9C85ebE478A1C0b69FCbb9'  # USDT на Arbitrum
balance = bot.onchain.get_balance(token=token_address)

# Проверить баланс другого кошелька
other_balance = bot.onchain.get_balance(address='0x742d35Cc6634C0532925a3b844Bc9e7595f0bEb5')
```

**Параметры:**

- `token` (optional): Объект Token, адрес контракта или None для нативного токена
- `address` (optional): Адрес кошелька для проверки (по умолчанию - адрес аккаунта)

**Возвращает:** Объект `Amount` с балансом

#### Метод send_token() - отправка токенов

Универсальный метод для отправки нативных и ERC-20 токенов:

```python
# Отправить нативный токен (ETH)
tx_hash = bot.onchain.send_token(
    to_address='0x742d35Cc6634C0532925a3b844Bc9e7595f0bEb5',
    amount=0.01
)
logger.info(f'Транзакция: {tx_hash}')

# Отправить ERC-20 токен (USDT)
tx_hash = bot.onchain.send_token(
    to_address='0x742d35Cc6634C0532925a3b844Bc9e7595f0bEb5',
    amount=10,
    token=Tokens.USDT_ARBITRUM_ONE
)

# Отправить весь баланс (amount=None)
tx_hash = bot.onchain.send_token(
    to_address='0x742d35Cc6634C0532925a3b844Bc9e7595f0bEb5',
    amount=None,  # отправит все доступные средства
    token=Tokens.USDT_ARBITRUM_ONE
)

# Отправить процент от баланса
balance = bot.onchain.get_balance()
amount_to_send = balance * 0.8  # 80% от баланса
tx_hash = bot.onchain.send_token(
    to_address='0x742d35Cc6634C0532925a3b844Bc9e7595f0bEb5',
    amount=amount_to_send.ether
)
```

**Параметры:**

- `to_address` (обязательный): Адрес получателя
- `amount`: Сумма для отправки (float, int, Amount или None для всего баланса)
- `token` (optional): Объект Token, адрес контракта или None для нативного токена

**Возвращает:** Хэш транзакции (строка)

**Особенности:**

- Автоматически проверяет достаточность средств
- Если средств недостаточно для ERC-20, отправляет весь доступный баланс
- Автоматически учитывает комиссию при отправке нативного токена
- Оптимизирует gas для экономии средств

#### Метод approve() - одобрение токенов

Одобрение токенов для использования смарт-контрактами (необходимо перед swap, bridge и т.д.):

```python
# Одобрить USDT для контракта DEX
bot.onchain.approve(
    token=Tokens.USDT_ARBITRUM_ONE,
    amount=100,  # одобрить 100 USDT
    spender='0xcontractAddress...'
)

# Одобрить неограниченное количество (infinite approve)
bot.onchain.approve(
    token=Tokens.USDT_ARBITRUM_ONE,
    amount=2**256 - 1,  # максимально возможная сумма
    spender='0xcontractAddress...'
)

# Отозвать одобрение (установить в 0)
bot.onchain.approve(
    token=Tokens.USDT_ARBITRUM_ONE,
    amount=0,
    spender='0xcontractAddress...'
)

# Использование с объектом ContractRaw
from models.contract_raw import ContractRaw

dex_contract = ContractRaw(
    address='0xcontractAddress...',
    abi_name='uniswap_router',
    chain=Chains.ARBITRUM_ONE
)

bot.onchain.approve(
    token=Tokens.USDT_ARBITRUM_ONE,
    amount=50,
    spender=dex_contract
)
```

**Параметры:**

- `token`: Объект Token или адрес контракта токена
- `amount`: Сумма одобрения (float, int, Amount)
- `spender`: Адрес контракта или объект ContractRaw

**Умное поведение:**

- Проверяет текущее одобрение перед отправкой транзакции
- Если уже одобрено достаточно, не отправляет транзакцию
- Автоматически пропускает для нативных токенов (не требуют approve)

#### Работа с газом и комиссиями

**Получение текущей цены газа:**

```python
# Получить цену газа в gwei
gas_price = bot.onchain.get_gas_price(gwei=True)
logger.info(f'Текущий газ: {gas_price} gwei')

# Получить цену газа в wei
gas_price_wei = bot.onchain.get_gas_price(gwei=False)
```

**Ожидание низкого газа:**

```python
# Ждать пока газ не упадет ниже 30 gwei
bot.onchain.gas_price_wait(gas_limit=30)
logger.info('Газ упал ниже 30 gwei, продолжаем работу')

# Использование лимита из config
bot.onchain.gas_price_wait()  # использует config.gas_price_limit
```

#### Работа с EIP-1559

Модуль автоматически определяет поддержку EIP-1559 и использует оптимальные параметры:

```python
# Проверить поддержку EIP-1559 вручную
is_eip1559 = bot.onchain.is_eip_1559()
logger.info(f'EIP-1559 supported: {is_eip1559}')

# Модуль автоматически использует:
# - Для EIP-1559 сетей: maxFeePerGas и maxPriorityFeePerGas
# - Для legacy сетей: gasPrice
```

**Как это работает:**

- Автоматически запрашивает `fee_history` для определения поддержки
- Рассчитывает оптимальную комиссию на основе медианы последних блоков
- Добавляет множитель для гарантии прохождения транзакции
- Кэширует результат для каждой сети

#### Продвинутые возможности

**Работа с кастомными контрактами:**

```python
from models.contract_raw import ContractRaw

# Создать объект контракта
my_contract = ContractRaw(
    address='0xcontractAddress...',
    abi_name='my_custom_abi',  # файл в config/data/ABIs/
    chain=Chains.ARBITRUM_ONE
)

# Получить экземпляр контракта Web3
contract_instance = bot.onchain._get_contract(my_contract)

# Вызвать view функцию
result = contract_instance.functions.myViewFunction().call()

# Отправить транзакцию
tx_params = bot.onchain._prepare_tx()
tx_params = contract_instance.functions.myFunction(param1, param2).build_transaction(tx_params)
bot.onchain._estimate_gas(tx_params)
tx_hash = bot.onchain._sign_and_send(tx_params)
```

**Получение приватного ключа из seed:**

```python
# Из строки
seed = "word1 word2 word3 ... word12"
private_key = bot.onchain.get_pk_from_seed(seed)
logger.info(f'Private key: {private_key}')

# Из списка
seed_list = ["word1", "word2", "word3", ... "word12"]
private_key = bot.onchain.get_pk_from_seed(seed_list)
```

**Удаление неиспользуемых approves:**

```python
# Требует ETHERSCAN_API_KEY в .env
# Получает все исторические Approval транзакции и отзывает их

bot.onchain.remove_approves()
# Автоматически:
# 1. Получает логи всех Approval через Etherscan API
# 2. Находит уникальные пары (токен, spender)
# 3. Отзывает каждое одобрение (устанавливает в 0)
```

#### Практические примеры

**Пример 1: Проверка баланса во всех сетях**

```python
with Bot(account) as bot:
    chains = Chains.get_chains_list()

    for chain in chains:
        bot.onchain.change_chain(chain)
        balance = bot.onchain.get_balance()
        logger.info(f'{chain.name}: {balance.ether} {chain.native_token}')

        # Записать в Excel
        bot.excel.set_cell(f'{chain.name}_balance', balance.ether)
```

**Пример 2: Отправка процента от баланса**

```python
with Bot(account) as bot:
    # Получить баланс
    balance = bot.onchain.get_balance()

    # Проверить минимум
    if balance.ether < 0.01:
        logger.error('Недостаточно средств')
        return

    # Отправить 90% баланса, оставить 10%
    amount_to_send = balance * 0.9

    tx_hash = bot.onchain.send_token(
        to_address='0x742d35Cc6634C0532925a3b844Bc9e7595f0bEb5',
        amount=amount_to_send.ether
    )

    logger.success(f'Отправлено {amount_to_send.ether} ETH')
```

**Пример 3: Swap через DEX (с approve)**

```python
with Bot(account) as bot:
    token_in = Tokens.USDT_ARBITRUM_ONE
    amount_in = 100

    # 1. Одобрить токен для DEX
    dex_router = '0xcontractAddress...'
    bot.onchain.approve(
        token=token_in,
        amount=amount_in,
        spender=dex_router
    )
    logger.info('Токен одобрен')

    # 2. Выполнить swap (через кастомный контракт или браузер)
    # ... код swap ...

    logger.success('Swap выполнен')
```

**Пример 4: Проверка балансов всех токенов во всех сетях для всех аккаунтов (без браузера)**

Этот пример демонстрирует мощный сценарий работы с Onchain без использования браузера - массовая проверка балансов с записью в Excel:

```python
from utils.utils import get_accounts
from core.onchain import Onchain
from core.excel import Excel
from config import Chains, Tokens

# Получить все аккаунты
accounts = get_accounts()

# Получить все сети и токены
chains = Chains.get_chains_list()
all_tokens = Tokens.get_tokens()

# Подключиться к Excel для записи результатов
excel = Excel(file='accounts.xlsx')

logger.info(f'Начинаем проверку балансов для {len(accounts)} аккаунтов')
logger.info(f'Сетей: {len(chains)}, Токенов: {len(all_tokens)}')

# Перебираем все аккаунты
for account in accounts:
    logger.info(f'Проверка аккаунта: {account.address}')

    # Подключаем аккаунт к Excel
    excel.connect_account(account)

    # Проверяем балансы во всех сетях
    for chain in chains:
        # Создаем Onchain для текущей сети (без браузера!)
        onchain = Onchain(account, chain)

        # Проверяем баланс нативного токена
        native_balance = onchain.get_balance()
        logger.info(f'{chain.name} - Native: {native_balance.ether} {chain.native_token}')

        # Записываем в Excel
        excel.set_cell(f'{chain.name}_Native', native_balance.ether)

        # Получаем токены для этой сети
        chain_tokens = Tokens.get_tokens_by_chain(chain)

        # Проверяем баланс каждого токена
        for token in chain_tokens:
            try:
                balance = onchain.get_balance(token=token)

                # Записываем только если баланс не нулевой
                if balance.ether > 0:
                    logger.info(f'{chain.name} - {token.symbol}: {balance.ether}')
                    excel.set_cell(f'{chain.name}_{token.symbol}', balance.ether)

            except Exception as e:
                logger.error(f'Ошибка при проверке {token.symbol} в {chain.name}: {e}')
                continue

    logger.success(f'Аккаунт {account.address} обработан')

logger.success('Проверка всех аккаунтов завершена!')
```

**Преимущества этого подхода:**

- ⚡ Максимальная скорость - нет затрат на открытие браузера
- 📊 Автоматическая запись в Excel для каждого аккаунта
- 🌐 Проверка всех сетей и токенов за один проход
- 🔄 Легко масштабируется на сотни аккаунтов
- 💾 Все результаты сохраняются в таблицу
- ❌ Не требует браузер (is_browser_run=False)

**Пример 5: Массовая рассылка токенов**

```python
with Bot(account) as bot:
    recipients = [
        '0xaddress1...',
        '0xaddress2...',
        '0xaddress3...'
    ]

    amount_per_wallet = 0.01  # по 0.01 ETH каждому

    for recipient in recipients:
        try:
            tx_hash = bot.onchain.send_token(
                to_address=recipient,
                amount=amount_per_wallet
            )
            logger.success(f'Отправлено на {recipient}: {tx_hash}')

            # Небольшая задержка между транзакциями
            random_sleep(5, 10)

        except Exception as e:
            logger.error(f'Ошибка отправки на {recipient}: {e}')
```

**Пример 5: Ожидание низкого газа перед транзакцией**

```python
with Bot(account) as bot:
    # Проверить текущий газ
    current_gas = bot.onchain.get_gas_price(gwei=True)
    logger.info(f'Текущий газ: {current_gas} gwei')

    # Если газ высокий, ждем
    if current_gas > 50:
        logger.warning('Газ слишком высокий, ждем снижения...')
        bot.onchain.gas_price_wait(gas_limit=30)

    # Отправить транзакцию
    bot.onchain.send_token(
        to_address='0x742d35Cc6634C0532925a3b844Bc9e7595f0bEb5',
        amount=0.1
    )
```

**Пример 6: Проверка баланса токенов портфолио**

```python
with Bot(account) as bot:
    # Список токенов для проверки
    tokens_to_check = [
        Tokens.USDT_ARBITRUM_ONE,
        Tokens.USDC_ARBITRUM_ONE,
        Tokens.ARB_ARBITRUM_ONE
    ]

    total_value = 0

    for token in tokens_to_check:
        balance = bot.onchain.get_balance(token=token)

        if balance.ether > 0:
            # Получить цену токена (если есть функция)
            # price = get_token_price(token.symbol)
            # value = balance.ether * price

            logger.info(f'{token.symbol}: {balance.ether}')
            bot.excel.set_cell(f'{token.symbol}_balance', balance.ether)
```

#### Особенности работы с различными сетями

**Optimism (L2) - учет L1 fee:**

Модуль автоматически учитывает дополнительную комиссию L1 для сети Optimism:

```python
# Для Optimism автоматически добавляется L1 fee
onchain = Onchain(account, Chains.OPTIMISM)
balance = onchain.get_balance()
tx_hash = onchain.send_token(
    to_address='0x...',
    amount=0.01
)
# L1 fee автоматически учтена в расчете комиссии
```

**Работа с прокси:**

```python
# В config/settings.py установите:
# is_web3_proxy = True

# Модуль автоматически использует прокси из account.proxy
# для всех RPC запросов
```

#### Важные замечания

1. **Приватный ключ обязателен**: Для отправки транзакций у аккаунта должен быть `private_key`

2. **Автоматическое определение адреса**: Если не указан `address`, он определяется из `private_key`

3. **Проверка сети токена**: Токен должен быть в той же сети, что и Onchain, иначе будет ошибка

4. **Оптимизация газа**: Модуль автоматически добавляет множитель 3-10% для гарантии прохождения транзакции

5. **Недостаток средств**: При отправке ERC-20, если средств недостаточно, отправляется весь доступный баланс

6. **Native token transfers**: При отправке нативного токена автоматически резервируется сумма на газ

#### Преимущества модуля Onchain

✅ Быстрая работа без браузера
✅ Полный контроль над транзакциями
✅ Автоматическая оптимизация комиссий
✅ Поддержка EIP-1559
✅ Умная обработка недостатка средств
✅ Работа со множеством сетей
✅ Безопасное хранение приватных ключей
✅ Детальное логирование всех операций

### Excel - модуль для работы с таблицами

Модуль `Excel` предоставляет удобный интерфейс для работы с Excel таблицами. Он используется для записи статистики, отчетности и хранения данных о выполненных операциях. Все операции с таблицами выполняются через библиотеку `openpyxl`.

#### Назначение модуля

Модуль Excel используется для:

- Записи статистики выполнения скриптов
- Хранения данных о транзакциях и операциях
- Отслеживания счетчиков (количество транзакций, swaps и т.д.)
- Работы с датами и временем последних операций
- Создания отчетов о работе скриптов
- Фильтрации аккаунтов для обработки

**Преимущества:**

- 📊 Автоматическое создание столбцов при необходимости
- 🔄 Автоматический поиск строки аккаунта по profile_number
- 💾 Мгновенное сохранение после каждой операции
- 📝 Удобная работа со счетчиками и датами
- 🗂️ Поддержка работы с несколькими таблицами
- ✨ Автоматическое создание таблицы, если её нет

#### Инициализация и подключение к таблице

**Важно:** При работе через `Bot`, модуль `excel` уже инициализирован и подключен к аккаунту. Он автоматически находит строку аккаунта в таблице по `profile_number`.

**Таблица по умолчанию:** В конфиге `config/settings.py` указывается путь к таблице Excel:

```python
# В config/settings.py
PATH_EXCEL = 'accounts.xlsx'  # Файл в директории config/data
```

**Использование через Bot:**

```python
def activity(bot: Bot):
    # bot.excel уже подключен к таблице и аккаунту
    # Автоматически найдена строка с profile_number аккаунта

    # Записать значение
    bot.excel.set_cell('Status', 'Working')

    # Прочитать значение
    status = bot.excel.get_cell('Status')
```

**Ручная инициализация:**

```python
from core.excel import Excel
from models.account import Account

# Вариант 1: С аккаунтом и стандартной таблицей
account = Account(profile_number=12345, private_key='0xabc...')
excel = Excel(account=account)

# Вариант 2: С указанием файла таблицы
excel = Excel(account=account, file='report.xlsx')

# Вариант 3: Без аккаунта (для общих операций)
excel = Excel(file='statistics.xlsx')
```

#### Методы чтения данных

**Метод get_cell() - чтение значения из ячейки:**

```python
# Прочитать значение из строки аккаунта
status = bot.excel.get_cell('Status')
logger.info(f'Статус: {status}')

# Прочитать значение из конкретной строки
value = bot.excel.get_cell('Balance', row=5)

# Если столбец не существует, он будет создан автоматически
balance = bot.excel.get_cell('ETH Balance')  # Создаст столбец, если его нет
```

**Метод get_column() - получение всех значений столбца:**

```python
# Получить все значения столбца
addresses = bot.excel.get_column('Address')
logger.info(f'Всего адресов: {len(addresses)}')

# Получить только непустые значения
addresses = bot.excel.get_column('Address', is_empty_pass=True)

# Пример: подсчет активных аккаунтов
statuses = bot.excel.get_column('Status', is_empty_pass=True)
active_count = statuses.count('Active')
logger.info(f'Активных аккаунтов: {active_count}')
```

**Метод get_row() - получение всей строки:**

```python
# Получить все значения из строки аккаунта
row_data = bot.excel.get_row()
logger.info(f'Данные строки: {row_data}')

# Получить конкретную строку
row_5 = bot.excel.get_row(row=5)
```

#### Методы записи данных

**Метод set_cell() - запись значения в ячейку:**

```python
# Записать в строку аккаунта
bot.excel.set_cell('Status', 'Completed')
bot.excel.set_cell('Balance', 1.234)
bot.excel.set_cell('TX Hash', '0xabc123...')

# Записать в конкретную строку
bot.excel.set_cell('Status', 'Working', row=10)

# Если столбец не существует, он создастся автоматически
bot.excel.set_cell('New Column', 'Some value')
```

**Метод add_row() - добавление новой строки:**

```python
# Добавить строку с данными
new_row = ['12345', '0xAddress...', 'password123', 'seed phrase']
bot.excel.add_row(new_row)

# Полезно для создания отчетов
report_data = [
    datetime.now().strftime('%Y-%m-%d'),
    'Swap',
    '0.1 ETH',
    'Success'
]
bot.excel.add_row(report_data)
```

#### Работа со счетчиками

Счетчики - это специальные ячейки для хранения числовых значений, которые можно увеличивать.

**Метод get_counter() - получение значения счетчика:**

```python
# Получить значение счетчика
swap_count = bot.excel.get_counter('Swaps')
logger.info(f'Выполнено swaps: {swap_count}')

# Если ячейка пустая, вернет 0 и запишет 0 в ячейку
tx_count = bot.excel.get_counter('TX Count')

# Работает с int и float
volume = bot.excel.get_counter('Total Volume')
```

**Метод increase_counter() - увеличение счетчика:**

```python
# Увеличить счетчик на 1
new_value = bot.excel.increase_counter('Swaps')
logger.info(f'Swaps: {new_value}')

# Увеличить на произвольное число
bot.excel.increase_counter('TX Count', number=5)

# Использование в логике
bot.onchain.send_token(to_address='0x...', amount=0.01)
bot.excel.increase_counter('Transactions')
logger.success('Транзакция выполнена и учтена')
```

**Метод get_counters() - получение всех счетчиков столбца:**

```python
# Получить все значения счетчиков
all_swaps = bot.excel.get_counters('Swaps')
logger.info(f'Всего swaps по всем аккаунтам: {sum(all_swaps)}')

# Найти среднее значение
average = sum(all_swaps) / len(all_swaps)
logger.info(f'Среднее количество swaps: {average:.2f}')

# Найти максимум
max_swaps = max(all_swaps)
logger.info(f'Максимум swaps: {max_swaps}')
```

#### Работа с датами

**Метод set_date() - запись текущей даты:**

```python
# Записать текущую дату и время
bot.excel.set_date('Last Activity')

# После выполнения операции
bot.onchain.send_token(to_address='0x...', amount=0.01)
bot.excel.set_date('Last TX Date')
logger.success('Дата транзакции записана')

# Формат даты настраивается в config/settings.py
# date_format = '%Y-%m-%d %H:%M:%S'
```

**Метод get_date() - чтение даты:**

```python
# Получить дату последней активности
last_date = bot.excel.get_date('Last Activity')
logger.info(f'Последняя активность: {last_date}')

# Проверить, давно ли была активность
from datetime import datetime, timedelta

last_tx = bot.excel.get_date('Last TX Date')
days_passed = (datetime.now() - last_tx).days

if days_passed > 7:
    logger.info('Прошло больше недели с последней транзакции')
    # Выполнить операцию
```

#### Переключение таблиц и аккаунтов

**Метод change_table() - смена таблицы:**

```python
# Переключиться на другую таблицу
bot.excel.change_table('report.xlsx')
bot.excel.set_cell('Date', datetime.now().strftime('%Y-%m-%d'))

# Вернуться к основной таблице
bot.excel.change_table('accounts.xlsx')

# Создать новую таблицу (если не существует)
bot.excel.change_table('new_report.xlsx')
```

**Метод connect_account() - переключение аккаунта:**

```python
from utils.utils import get_accounts

# Полезно при работе с несколькими аккаунтами
accounts = get_accounts()
excel = Excel(file='accounts.xlsx')

for account in accounts:
    # Подключаем аккаунт к Excel
    excel.connect_account(account)

    # Теперь все операции будут для этого аккаунта
    status = excel.get_cell('Status')
    if status == 'Work':
        # Выполнить операции
        excel.set_cell('Status', 'Processing')
```

#### Практические примеры

**Пример 1: Базовая статистика после операций**

```python
def activity(bot: Bot):
    # Выполняем swap
    # ... код swap ...

    # Записываем статистику
    bot.excel.increase_counter('Swaps')
    bot.excel.set_date('Last Swap')
    bot.excel.set_cell('Last Swap Amount', 100)
    bot.excel.set_cell('Status', 'Active')

    logger.success('Статистика обновлена')
```

**Пример 2: Фильтрация аккаунтов по условиям**

```python
from utils.utils import get_accounts
from datetime import datetime, timedelta

def filter_accounts():
    accounts = get_accounts()
    excel = Excel(file='accounts.xlsx')

    filtered = []
    limit_date = datetime.now() - timedelta(days=1)

    for account in accounts:
        excel.connect_account(account)

        # Проверяем условия
        status = excel.get_cell('Status')
        swap_count = excel.get_counter('Swaps')
        last_date = excel.get_date('Last Activity')

        # Фильтруем
        if status == 'Work' and swap_count < 10 and last_date < limit_date:
            filtered.append(account)

    logger.info(f'Отфильтровано {len(filtered)} аккаунтов')
    return filtered
```

**Пример 3: Создание отчета в отдельной таблице**

```python
def activity(bot: Bot):
    # Основная работа с аккаунтом
    balance = bot.onchain.get_balance()

    # Записываем в основную таблицу
    bot.excel.set_cell('Balance', balance.ether)

    # Создаем отчет в отдельной таблице
    bot.excel.change_table('daily_report.xlsx')

    # Добавляем строку отчета
    report_row = [
        datetime.now().strftime('%Y-%m-%d %H:%M'),
        bot.account.address,
        balance.ether,
        'Success'
    ]
    bot.excel.add_row(report_row)

    # Возвращаемся к основной таблице
    bot.excel.change_table('accounts.xlsx')
```

**Пример 4: Подсчет общей статистики**

```python
def calculate_statistics():
    excel = Excel(file='accounts.xlsx')

    # Получаем все счетчики
    all_swaps = excel.get_counters('Swaps')
    all_txs = excel.get_counters('Transactions')

    # Подсчитываем статистику
    total_swaps = sum(all_swaps)
    total_txs = sum(all_txs)
    avg_swaps = total_swaps / len(all_swaps) if all_swaps else 0

    logger.info(f'Всего swaps: {total_swaps}')
    logger.info(f'Всего транзакций: {total_txs}')
    logger.info(f'Среднее swaps на аккаунт: {avg_swaps:.2f}')

    # Создаем итоговый отчет
    excel.change_table('summary.xlsx')
    excel.add_row([
        datetime.now().strftime('%Y-%m-%d'),
        total_swaps,
        total_txs,
        avg_swaps
    ])
```

**Пример 5: Отслеживание балансов**

```python
def activity(bot: Bot):
    # Проверяем балансы разных токенов
    eth_balance = bot.onchain.get_balance()
    bot.excel.set_cell('ETH Balance', eth_balance.ether)

    # Переключаемся на другие сети
    bot.onchain.change_chain(Chains.ARBITRUM_ONE)
    arb_balance = bot.onchain.get_balance()
    bot.excel.set_cell('ARB ETH Balance', arb_balance.ether)

    bot.onchain.change_chain(Chains.OPTIMISM)
    op_balance = bot.onchain.get_balance()
    bot.excel.set_cell('OP ETH Balance', op_balance.ether)

    # Записываем дату проверки
    bot.excel.set_date('Last Balance Check')

    logger.success('Балансы обновлены в таблице')
```

#### Паттерны использования Excel

**Паттерн 1: Отчетность по активностям**

```python
def activity(bot: Bot):
    try:
        # Выполняем основную логику
        bot.onchain.send_token(to_address='0x...', amount=0.01)

        # Успешно - обновляем статистику
        bot.excel.increase_counter('Successful TX')
        bot.excel.set_date('Last Success')
        bot.excel.set_cell('Status', 'Active')

    except Exception as e:
        # Ошибка - записываем
        bot.excel.increase_counter('Failed TX')
        bot.excel.set_cell('Last Error', str(e))
        bot.excel.set_date('Last Error Date')
        logger.error(f'Ошибка: {e}')
```

**Паттерн 2: Работа с лимитами**

```python
def activity(bot: Bot):
    # Проверяем лимит операций
    swap_count = bot.excel.get_counter('Swaps')
    max_swaps = 10

    if swap_count >= max_swaps:
        logger.warning(f'Достигнут лимит swaps: {swap_count}/{max_swaps}')
        bot.excel.set_cell('Status', 'Limit Reached')
        return

    # Выполняем операцию
    # ... код swap ...

    bot.excel.increase_counter('Swaps')
    logger.info(f'Swaps: {swap_count + 1}/{max_swaps}')
```

**Паттерн 3: Временные интервалы между операциями**

```python
def activity(bot: Bot):
    from datetime import datetime, timedelta

    # Проверяем время последней операции
    last_tx = bot.excel.get_date('Last TX')
    min_interval = timedelta(hours=6)

    time_passed = datetime.now() - last_tx

    if time_passed < min_interval:
        remaining = min_interval - time_passed
        logger.info(f'Слишком рано. Осталось: {remaining}')
        return

    # Выполняем операцию
    # ... код операции ...

    bot.excel.set_date('Last TX')
```

#### Автоматические функции Excel

**Автоматическое создание столбцов:**

Если вы обращаетесь к несуществующему столбцу через `set_cell()`, `get_cell()` или другие методы, столбец создастся автоматически:

```python
# Столбец "New Metric" создастся автоматически
bot.excel.set_cell('New Metric', 42)
```

**Автоматический поиск строки аккаунта:**

При инициализации Excel через Bot, модуль автоматически находит строку с `profile_number` аккаунта. Если строки нет, она создается:

```python
# Не нужно искать строку вручную
# bot.excel автоматически знает, в какую строку писать
bot.excel.set_cell('Status', 'Working')
```

**Автоматическое сохранение:**

После каждой операции записи файл автоматически сохраняется:

```python
bot.excel.set_cell('Balance', 1.5)  # Сохранено
bot.excel.increase_counter('TX')     # Сохранено
bot.excel.set_date('Date')           # Сохранено
```

#### Преимущества модуля Excel

✅ Автоматическое создание столбцов и строк
✅ Мгновенное сохранение после каждой операции
✅ Удобная работа со счетчиками
✅ Поддержка дат с настраиваемым форматом
✅ Возможность работы с несколькими таблицами
✅ Автоматический поиск строки аккаунта
✅ Простой API для всех операций
✅ Поддержка фильтрации и аналитики

## 📚 Примеры использования

Ниже приведены базовые примеры для быстрого старта.

**Важно:** Весь код размещается в функции `activity(bot: Bot)` в файле `run.py`. Объект `bot` уже создан и передан в функцию как параметр. Модуль `bot.onchain` по умолчанию подключен к сети, указанной в `config.start_chain` (настраивается в `config/settings.py`).

### Пример 1: Простой запуск профиля и открытие сайта

Самый простой пример - открыть браузер и перейти на сайт:

```python
def activity(bot: Bot):
    # Открыть сайт
    bot.ads.open_url('https://google.com')

    # Подождать 5 секунд
    bot.ads.page.wait_for_timeout(5000)

    # Кликнуть на элемент
    bot.ads.click('text=Поиск')

    # Ввести текст в поле
    bot.ads.type_text('input[name="q"]', 'Web3 automation')

    # Записать результат в Excel
    bot.excel.set_cell('Status', 'Completed')
```

### Пример 2: Проверка баланса

Проверка баланса нативного токена и ERC-20 токенов:

```python
def activity(bot: Bot):
    # Проверить баланс нативного токена (ETH)
    native_balance = bot.onchain.get_balance()
    logger.info(f'Баланс ETH: {native_balance}')

    # Проверить баланс USDT
    usdt_balance = bot.onchain.get_balance(token=Tokens.USDT_ARBITRUM_ONE)
    logger.info(f'Баланс USDT: {usdt_balance}')

    # Записать балансы в Excel
    bot.excel.set_cell('ETH Balance', native_balance)
    bot.excel.set_cell('USDT Balance', usdt_balance)

    # Проверить балансы во всех сетях
    chains = Chains.get_chains_list()
    for chain in chains:
        onchain = Onchain(bot.account, chain)
        balance = onchain.get_balance()
        logger.info(f'Баланс в {chain.name}: {balance}')
```

### Пример 3: Отправка токенов

Отправка нативных токенов и ERC-20:

```python
def activity(bot: Bot):
    # Получить баланс
    balance = bot.onchain.get_balance()

    # Отправить 50% баланса
    amount = balance * 0.5
    recipient = '0x742d35Cc6634C0532925a3b844Bc9e7595f0bEb5'

    # Отправить ETH
    bot.onchain.send_token(
        amount=amount,
        to_address=recipient
    )

    # Отправить USDT
    bot.onchain.send_token(
        token=Tokens.USDT_ARBITRUM_ONE,
        amount=10,
        to_address=recipient
    )

    # Записать в Excel
    bot.excel.set_cell('Last TX', 'Sent')
    bot.excel.set_date('TX Date')
    bot.excel.increase_counter('TX Count')
```

### Пример 4: Работа с Metamask

Авторизация в Metamask и взаимодействие с dApp:

```python
def activity(bot: Bot):
    # Импортировать кошелек (если еще не импортирован)
    if not bot.metamask.is_wallet_created():
        bot.metamask.import_wallet(
            seed=bot.account.seed,
            password=bot.account.password
        )

    # Авторизоваться в Metamask
    bot.metamask.auth_metamask()

    # Переключить сеть
    bot.metamask.select_chain(Chains.ARBITRUM_ONE)

    # Открыть dApp
    bot.ads.open_url('https://app.uniswap.org')

    # Подключить кошелек к dApp
    bot.metamask.connect()

    # Подписать сообщение
    bot.metamask.sign()

    # Подтвердить транзакцию
    bot.metamask.send_tx()
```

### Пример 5: Работа с Excel

Запись и чтение данных из Excel:

```python
def activity(bot: Bot):
    # Записать значение в ячейку
    bot.excel.set_cell('Status', 'Working')

    # Прочитать значение из ячейки
    status = bot.excel.get_cell('Status')
    logger.info(f'Статус: {status}')

    # Увеличить счетчик
    bot.excel.increase_counter('Swap Count')

    # Получить значение счетчика
    swap_count = bot.excel.get_counter('Swap Count')
    logger.info(f'Количество свопов: {swap_count}')

    # Записать текущую дату
    bot.excel.set_date('Last Activity')

    # Получить дату
    last_date = bot.excel.get_date('Last Activity')

    # Добавить новую строку
    bot.excel.add_row(['Account1', '100', 'Active'])

    # Работа с другой таблицей
    bot.excel.change_table('statistics.xlsx', 'Sheet1')
    bot.excel.set_cell('Total', 500)
```

### Пример 6: Вывод с биржи

Вывод токенов с OKX или Binance:

```python
def activity(bot: Bot):
    # Вывод USDT с OKX на Arbitrum
    bot.exchanges.okx.withdraw(
        token=Tokens.USDT_ARBITRUM_ONE,
        amount=10,
        chain=Chains.ARBITRUM_ONE,
        to_address=bot.account.address
    )

    # Вывод ETH с Binance
    bot.exchanges.binance.withdraw(
        token='ETH',
        amount=0.01,
        chain=Chains.ARBITRUM_ONE,
        to_address=bot.account.address
    )

    # Получить список доступных сетей на бирже
    okx_chains = bot.exchanges.okx.get_chains()
    logger.info(f'Доступные сети на OKX: {okx_chains}')

    # Записать в Excel
    bot.excel.set_cell('Withdraw', 'Success')
    bot.excel.set_date('Withdraw Date')
```

### Пример 7: Комплексный сценарий

Полный цикл: вывод с биржи, проверка баланса, отправка:

```python
def activity(bot: Bot):
    try:
        # 1. Вывести с биржи
        logger.info("Вывод с OKX...")
        bot.exchanges.okx.withdraw(
            token=Tokens.USDT_ARBITRUM_ONE,
            amount=50,
            chain=Chains.ARBITRUM_ONE,
            to_address=bot.account.address
        )

        # 2. Подождать поступления
        random_sleep(300, 600)  # 5-10 минут

        # 3. Проверить баланс
        balance = bot.onchain.get_balance(token=Tokens.USDT_ARBITRUM_ONE)
        logger.success(f'Получено USDT: {balance}')

        # 4. Отправить на другой адрес
        if balance > 0:
            bot.onchain.send_token(
                token=Tokens.USDT_ARBITRUM_ONE,
                amount=balance * 0.95,  # 95% баланса
                to_address='0x742d35Cc6634C0532925a3b844Bc9e7595f0bEb5'
            )

        # 5. Записать результат
        bot.excel.set_cell('Status', 'Success')
        bot.excel.increase_counter('Completed')
        bot.excel.set_date('Last Success')

    except Exception as e:
        logger.error(f'Ошибка: {e}')
        bot.excel.set_cell('Status', 'Failed')
        bot.excel.set_cell('Error', str(e))
```

### Полезные советы

1. **Используйте try-except** для обработки ошибок
2. **Записывайте результаты в Excel** для отчетности
3. **Добавляйте задержки** между действиями (`random_sleep()`)
4. **Проверяйте балансы** перед отправкой
5. **Используйте логирование** для отладки (`logger.info()`, `logger.error()`)

Больше примеров смотрите в директории `snippets/`.
